# `/os` Dashboard — Example Output

> **Illustrativ / Illustrative.** Dies ist eine statische Beispiel-Ausgabe von `/os` für einen Vault, der nur mit den
> mitgelieferten Beispiel-Notizen befüllt ist. Die Zahlen sind erfunden und intern konsistent, kein echter Stand. /
> This is a static example of what `/os` prints for a vault seeded only with the bundled example notes. The numbers are
> invented and internally consistent, not a real state.

```
PERSONAL OS — Dashboard (2025-01-15)
────────────────────────────────────────────────────────────
12 Lessons · 4 Ideas · 5 Knowledge · 3 Projects · 1 Profile

Neueste Lessons / Newest lessons
  • [[cap-llm-api-costs-with-a-hard-limit]]   — set a hard spend cap before any LLM call
  • [[never-force-push-a-shared-branch]]      — use --force-with-lease, never --force
  • [[verify-the-deploy-not-just-the-merge]]  — a green merge is not a green deploy
  • [[read-the-error-message-before-changing-code]] — reproduce + read before editing

Health
  18 lesson activations in the last 30 days · 0 stale · 1 archive candidate

Vorgeschlagene nächste Schritte / Suggested next steps
  • Skizziere den [[weekly-review-command]] — eine Wochenzusammenfassung der Lessons.
    Sketch the [[weekly-review-command]] — a weekly summary of lessons.
  • Verwandle die meistgenutzte Lesson in einen Tipp: [[share-a-lesson-as-a-tip]].
    Turn the top-fired lesson into a tip: [[share-a-lesson-as-a-tip]].
  • Prüfe den 1 Archiv-Kandidaten und archiviere ihn oder re-validiere ihn.
    Review the 1 archive candidate and either archive or re-validate it.
────────────────────────────────────────────────────────────
```

Hinweis / Note: Der frisch aufgesetzte Vault ist mit 4 Beispiel-Lessons geseedet, die oben unter
"Neueste Lessons" verlinkt sind; die übrigen Zählwerte (12 Lessons gesamt, 4 Ideas, …) stehen für einen
gewachsenen Vault und sind hier nur zur Veranschaulichung gezeigt. / The freshly set-up vault is seeded with the
4 example lessons linked under "Newest lessons" above; the remaining counts (12 lessons total, 4 ideas, …) represent a
grown vault and are shown here purely for illustration.
